class Solution{
    valueEqualToIndex(arr,n){
        //code here
        const ans = []
        for(let i=0; i<n; i++){
            if(arr[i] === i+1) ans.push(arr[i])
        }
        
        return ans
    }
}
