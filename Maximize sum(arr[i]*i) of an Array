class Solution {
    Maximize(arr,n){
       //code here
       arr = arr.sort((a,b) => a-b);
       let sum =0
       for(let i=0; i<arr.length; i++){
           sum += arr[i] * i
       }
       
       return sum % (10**9 + 7)
    }
}
